# Plan — coffer-alert-text-only

**Project:** cata-runelite-plugin
**Task:** coffer-alert-text-only
**Branch:** bugfix/coffer-alert-text-only
**Planned:** 2026-09-12T18:19:47Z

---

## Approach

Add a no-image overload to `AlertsApiClient.sendAlert(...)`, keeping the existing image-carrying overload for loot/death unchanged, sharing the request-building/response-handling logic between both rather than duplicating it. Update `CofferAlertListener` to call the new overload directly instead of routing through `ScreenshotCapture`, dropping that dependency entirely from the class.

## Layers affected

- API client (`api/AlertsApiClient.java`)
- Alerts (`alerts/CofferAlertListener.java`)

## Conventions to follow

- Allman brace style, tab indentation
- `ApiCallback<T>` / `ApiErrorType` async result pattern
- No `var` usage — explicit types throughout

## Concerns to watch

- **WRN-005** — `AlertsApiClient` already duplicates `EventsApiClient`'s error-classification logic inline. Adding a second overload doesn't fix that, but must not make it worse — share the response-handling logic between both `sendAlert` overloads rather than duplicating it a third time.

---

## Steps

### Step 1 — No-image overload on AlertsApiClient
`api/AlertsApiClient.java`: add `sendAlert(String token, AlertKind kind, String summary, ApiCallback<Void> callback)` (no `pngBytes` param). Refactor both overloads to share one private request-building/response-handling path, parameterized on an optional image part, instead of duplicating the OkHttp `Callback` anonymous class a second time.

### Step 2 — Drop screenshot capture from CofferAlertListener
`alerts/CofferAlertListener.java`: remove the `ScreenshotCapture` field and constructor param entirely; call the new no-image `sendAlert(...)` overload directly with the matched chat message text.

### Step 3 — Docs
`README.md`: update the "Discord alerts" section's coffer bullet to note it's text-only (no screenshot), and mention the catabot-side companion change (making `image` optional on `POST /alerts` for `kind=coffer`, dropping the misleading RSN prefix for coffer) is tracked separately in that repo's own `.mflow`.

### Step 4 — Manual QA
`./gradlew compileJava` to confirm it compiles clean. No live game session available in this environment — full in-game confirmation (coffer alert posts text-only, loot/death alerts unaffected) is on the developer before shipping.

---

## Definition of done

- [ ] All steps completed
- [ ] Follows existing conventions
- [ ] Unit tests written and passing
- [ ] No new concerns introduced
- [ ] Ready for `mflow verify-task`

---
_Generated by mflow plan-task — 2026-09-12T18:19:47Z_
