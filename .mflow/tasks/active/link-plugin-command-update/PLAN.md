# Plan — link-plugin-command-update

**Project:** cata-runelite-plugin
**Task:** link-plugin-command-update
**Branch:** bugfix/link-plugin-command-update
**Planned:** 2026-09-16T13:58:44Z

---

## Approach

Straightforward text-only fix: update the three live user-facing strings to reference `/link-plugin` (a slash command with a required `rsn` option) instead of the retired `!link` prefix command, then compile to confirm nothing else broke. `poc-runelite-plugin.md` is left alone per the intake decision.

## Layers affected

- Config (`CatastrophicEventsConfig.java`)
- UI (`CatastrophicEventsPanel.java`)
- Docs (`README.md`)

## Conventions to follow

No code-structure conventions apply here (pure string changes); match existing tone/phrasing in each file.

## Concerns to watch

None identified — no logic changes, no new dependencies.

---

## Steps

### Step 1 — Config description
`src/main/java/com/catastrophic/events/CatastrophicEventsConfig.java:14` — change the "Plugin token" `description` from `"Paste the token DM'd to you by the !link command in Discord."` to `"Paste the token DM'd to you by the /link-plugin command in Discord."`

### Step 2 — NOT_LINKED panel message
`src/main/java/com/catastrophic/events/ui/CatastrophicEventsPanel.java:152` — change the NOT_LINKED message from `"Run !link in Discord, then paste the token it DMs you here via Event Setup below."` to `"Run /link-plugin in Discord (with your RSN), then paste the token it DMs you here via Event Setup below."`

### Step 3 — README
`README.md:130` — change `` `!link <rsn>` `` to `` `/link-plugin <rsn>` ``, adjusting surrounding wording since it's now a slash command, not a typed prefix command.

### Step 4 — Manual QA
`./gradlew compileJava` to confirm it compiles clean.

---

## Definition of done

- [ ] All steps completed
- [ ] Follows existing conventions
- [ ] Unit tests written and passing
- [ ] No new concerns introduced
- [ ] Ready for `mflow verify-task`

---
_Generated by mflow plan-task — 2026-09-16T13:58:44Z_
