# Plan — remove-stackable-loot-exclusion

**Project:** cata-runelite-plugin
**Task:** remove-stackable-loot-exclusion
**Branch:** bugfix/remove-stackable-loot-exclusion
**Planned:** 2026-09-23T15:03:02Z

---

## Approach

Drop the `!composition.isStackable()` guard from `LootAlertListener.handleLoot()`'s value check, so the existing `totalValue >= VALUE_THRESHOLD_GP` check runs for every item regardless of stackability. Update the class Javadoc, which currently documents the check as "non-stackable" only, to match the new behavior.

## Layers affected

- Alerts (`LootAlertListener.java` only)

## Conventions to follow

- Allman brace style, tab indentation
- No new config toggle or allowlist — this is a threshold-check simplification, not a new feature

## Concerns to watch

None identified directly. WRN-008 (deprecated `net.runelite.api.ItemID`) touches the same file but is unrelated/out of scope here.

---

## Steps

### Step 1 — Remove the stackable exclusion
`LootAlertListener.java:99`: change `if (config.lootSharingEnabled() && !composition.isStackable())` to `if (config.lootSharingEnabled())`, so the total-stack-value check (`price * quantity >= 1.5m gp`) runs for every item.

### Step 2 — Update the class Javadoc
`LootAlertListener.java`: update the class-level Javadoc (currently "any single non-stackable drop worth 1.5m gp or more") to describe the check as applying to any item's total stack value, stackable or not.

### Step 3 — Compile check
`./gradlew compileJava` to confirm it compiles clean.

---

## Definition of done

- [ ] All steps completed
- [ ] Follows existing conventions
- [ ] Unit tests written and passing
- [ ] No new concerns introduced
- [ ] Ready for `mflow verify-task`

---
_Generated by mflow plan-task — 2026-09-23T15:03:02Z_
